version: '3.8'

services:
  web:
    build: .
    image: assignment5-flask:latest
    ports:
      - "5000:5000"
    volumes:
      - ./projects.db:/app/projects.db
      - ./static/images:/app/static/images
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
